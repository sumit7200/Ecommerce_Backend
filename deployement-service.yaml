apiVersion: v1kind: Servicemetadata:  name: my-servicespec:  selector:    app: my-app  # Change this to match the labels on your Pod  ports:    - protocol: TCP      port: 80  # Port on the service      targetPort: 80  # Port on the Pod